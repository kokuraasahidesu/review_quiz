# 统一换行符：仓库里一律 LF，避免 Windows 提交时出现"整文件都改了"的假 diff
* text=auto eol=lf

# 成品 HTML 与 md 都按文本处理（diff 可读）
*.html text eol=lf
*.md   text eol=lf
*.js   text eol=lf
*.txt  text eol=lf

# 二进制：别做换行转换，也别让它进 diff
*.png  binary
*.docx binary
*.ico  binary

# ⚠ 例外：这两个固件**故意是 CRLF**（用来测"CRLF + GBK / CRLF + BOM 能不能正确解码"），
#   一旦被规范化成 LF，对应测试就变成"测空气"。所以显式关掉一切转换。
fixtures/txt/crlf_* -text
