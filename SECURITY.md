# 安全性说明（SECURITY）

> 一句话：**别人递给你的一切都是"数据"** —— 题面、选项、解析、分享文件里的载荷，全都只当文字显示；
> 应用不做任何"从数据到代码"的转换（没有 `eval`，没有把用户内容拼进 `innerHTML`，解析 docx 也不用 XML 解析器）。
> 但**分享文件本身是一个 HTML 程序**：从不可信来源打开任何 HTML 都有固有风险，见下面「仍然存在的风险」。

## 威胁模型：谁能递给我什么

| 来源 | 他能控制什么 | 他能碰到什么 |
|---|---|---|
| **一份 txt / docx 题库** | 全部题面、选项、解析文字；docx 里还有 zip 结构与 XML | 你的导入流程、浏览器内存（他碰不到你的文件系统） |
| **别人做好的分享文件**（`*.html`） | 整个文件：应用代码、载荷 JSON、任何 HTML/JS | 打开它的那个浏览器页面（等同"运行一个陌生网页"） |
| **AI 服务商的返回内容** | 生成的解析 / 总评文本 | 只落进"草案"，要你点「确认入库」（同样是文字） |
| 你自己 | —— | 本机 localStorage（题库 / 进度 / 错题 / Key） |

## 已验证的防护（都能现场重跑）

### 1) 渲染：用户内容永远是文字

- `ui/attempt-view.js`、`ui/wrong-view.js` **没有一处 `innerHTML`**；取数一律 `textContent`。
- 唯一 3 处 `innerHTML` 在 `ui/review-panel.js`，都是**静态骨架字符串**，里面不拼任何题目字段
  （`verify/review.test.js` 用"只有 3 处、且都是字面量"的断言钉住）。
- **实证**：`verify/inject-gen.js` + `verify/inject-parse.js` 造了一份"题面/选项/解析全是攻击性标记"的
  题库（`<script>`、`<img onerror>`、`<svg onload>`、`<iframe src=javascript:>`、`<object data=…>`、
  `{{constructor.constructor(...)()}}`、`${alert(1)}`），用**真实解析器 + 真实答题界面**跑一遍：
  界面里 `script/img/svg/iframe/object/embed/form` 元素数 = **0**，canary 数组 = **0**（没有任何代码被执行），
  而 `<script>…</script>` 这段原文**照常当文字显示**在题面里。

### 2) 解析 docx：不用 XML 解析器 → XXE / 实体扩展**结构性不可能**

- `core/parse/docx.js` 用正则 + `TextDecoder` 逐层切分，**从不调用 `DOMParser`**
  （这样同一份代码也能在 Node 里跑）。
- 实体解码只认一小张白名单表（`&lt; &gt; &quot; &apos;` + 数字引用），**没有外部实体、没有 DTD 解析**。
- 越界数字引用（`&#99999999;`、`&#x110000;`）换算越界字符：现在一律落成 U+FFFD，**不再抛异常打断导入**。

### 3) 分享文件：载荷是数据，且结构不可被题面破坏

- 内嵌载荷时把 `<` 转义成 `\u003c`、`\u2028/\u2029` 也转义 → 题面里写 `</script>` 也**截不断文件**
  （`core/data.js` 的 `payloadBlock`）。
- 导出前过三道闸门：**脱敏白名单**（`SHARE_EXAM_FIELDS` / `SHARE_QUESTION_FIELDS`，一个不多一个不少）、
  **敏感扫描**（密钥形状 / 敏感字段名，含全角同形字）、**外部引用自查**（有 `<script src>`/`<link>` 一律拒发）。
- **实证**：同一份攻击性题库导出后，分享文件里 `<img` / `<svg` / `<iframe` 元素数与**原壳完全一致**，
  只多出 1 个 `<script>`（就是载荷块本身），题面照常渲染成文字。

### 4) 接收侧：白名单在"**使用**"这道边界上

- `extractPayload` 是**原样读**（只做 `JSON.parse`）；真正的收敛发生在
  `SchemaCore.createQuestion` 与 `QuizCore.resolveConfig`：未知键、`onclick`、`__proto__`、超深嵌套
  一律进不了模型；恶意配置里的 `autoNextMs=99999` 会被用时校验拦下并落回默认（不会真等 100 秒）。
- 反序列化**不污染原型**：`({}).polluted === undefined` 在解析前后都成立。

### 5) 拒绝服务（DoS）：三处"别人能撑死我"的入口都有上限

| 入口 | 上限 | 行为 |
|---|---|---|
| docx 里某一段解压后的大小 | `ZipCore.INFLATE_MAX_BYTES` = **64 MB** | 报 `E_TOO_LARGE` + 人话建议；**流式累计**，不信中央目录里可撒谎的"声明大小" |
| 分享文件里的载荷体积 | `DataCore.MAX_PAYLOAD_BYTES` = **32 MB** | 拒载并说明（"这么大的试卷不正常"） |
| 题库列表项 / 载荷嵌套深度 | `MAX_LIST_ITEMS` = 20000；`STRIP_MAX_DEPTH` = 32 层 | 截断 / fail-closed 丢弃 |

证据：`verify/safety.test.js`（35 条，含自造 zip 的"炸弹"与越界实体用例）、
`verify/probe-safety-old.js`（4 条"改坏必红"）。

## 本次审计发现并修掉的问题

1. **压缩炸弹**（中）：`inflateRaw` 原来不设上限 —— 一份几百 KB 的 docx 可以解压出几 GB，导入即卡死/爆内存。
   已加流式上限 64 MB。
2. **越界数字实体**（低）：`String.fromCodePoint(99999999)` 抛 `RangeError`，一份改过的 docx 就能让导入失败。
   已兜住并按 U+FFFD 处理。
3. **超大载荷**（中）：别人给的分享文件可以塞几百 MB 的 JSON，收件人一打开就卡住。已加 32 MB 上限并拒载。

## 仍然存在的风险（如实列出，不粉饰）

1. **打开任何来源不明的 HTML，本身就是运行陌生人写的程序。** 本应用导出的文件是安全的（上面的证据），
   但**伪装成"试卷.html"的恶意文件**不在本应用控制范围内 —— 它可以是任意网页。请只打开你信任的人给的分享文件。
   同理：`file://` 下不同浏览器对"本地页面能否读取相邻文件"策略不同（有的允许读同目录文件），
   这也是"不要随便打开陌生 HTML"的另一个理由。
2. **界面会卡**：超大题库（十几万字以上）在导入/校对时明显变慢；这是纯前端单线程的取舍，不是漏洞。
3. **本机存储**：题库/进度/错题/API Key 全在浏览器 localStorage，**同一台电脑上的其它网页/其它人**在
   同一浏览器配置下可以看到它们；隐私模式 / 清缓存会丢。别在公用电脑上放含密钥的题库。
4. **AI 功能会把内容发给你选的服务商**：题干/答案会离开本机（这是功能本身，不是缺陷）；不填 Key 就不用。
5. **没有 CSP、没有 SRI**：单文件离线应用没有服务器下发响应头，安全边界靠"不把数据当代码"这条纪律
   与上面的断言来维持。如果你要把它放到网站上托管，请自行加 CSP 等响应头。
6. **AI 生成内容可能有错**：不要把它当标准答案（见 README 的「常见问题」）。

## 报告安全问题

发现绕过上述防护的构造，请发邮件到 <liyi07@outlook.com>（附上最小复现文件与
`复制诊断信息` 里的那段文本更好）。我会先复现、再补一条**能变红**的断言，然后修。
